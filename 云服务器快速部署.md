# ⚡ 云服务器快速部署（5分钟）

## 🎯 最快部署方式

### 方法 1：一键部署脚本（推荐）

在你的云服务器上运行：

```bash
# 下载并运行一键部署脚本
curl -fsSL https://raw.githubusercontent.com/tanzhijanshifeifuhua/github-navigator/main/scripts/auto_deploy.sh -o deploy.sh
chmod +x deploy.sh
sudo bash deploy.sh
```

按提示选择：
- 选项 1: Docker 部署（推荐，最简单）
- 选项 2: 传统部署（更灵活）

等待 3-5 分钟，完成！

---

### 方法 2：手动 Docker 部署

```bash
# 1. 安装 Docker
curl -fsSL https://get.docker.com | sh
systemctl start docker

# 2. 克隆项目
cd /opt
git clone https://github.com/tanzhijanshifeifuhua/github-navigator.git
cd github-navigator

# 3. 启动服务
docker-compose -f config/docker-compose.yml up -d

# 4. 访问
# http://你的服务器IP:5000
```

---

## 📋 准备工作

### 1. 购买云服务器

**推荐配置：**
- CPU: 1核
- 内存: 2GB
- 系统: Ubuntu 20.04
- 带宽: 1Mbps

**推荐平台：**
- 阿里云：https://www.aliyun.com
- 腾讯云：https://cloud.tencent.com
- 华为云：https://www.huaweicloud.com

### 2. 配置安全组

开放端口：
- 22 (SSH)
- 80 (HTTP)
- 443 (HTTPS)
- 5000 (应用端口)

### 3. 连接服务器

**Windows 用户：**
- 使用 PuTTY 或 Xshell
- 或使用 Windows Terminal + SSH

**Mac/Linux 用户：**
```bash
ssh root@你的服务器IP
```

---

## 🚀 部署步骤

### Docker 方式（推荐）

```bash
# 1. 安装 Docker
curl -fsSL https://get.docker.com | sh
systemctl start docker
systemctl enable docker

# 2. 安装 Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# 3. 克隆项目
cd /opt
git clone https://github.com/tanzhijanshifeifuhua/github-navigator.git
cd github-navigator

# 4. 启动服务
docker-compose -f config/docker-compose.yml up -d

# 5. 查看日志
docker-compose -f config/docker-compose.yml logs -f
```

### 传统方式

```bash
# 1. 安装依赖
apt update && apt upgrade -y
apt install python3 python3-pip python3-venv nginx git -y

# 2. 克隆项目
cd /var/www
git clone https://github.com/tanzhijanshifeifuhua/github-navigator.git
cd github-navigator

# 3. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 4. 配置服务
cp config/github-navigator.service /etc/systemd/system/
systemctl daemon-reload
systemctl start github-navigator
systemctl enable github-navigator

# 5. 配置 Nginx
cp config/nginx.conf /etc/nginx/sites-available/github-navigator
ln -s /etc/nginx/sites-available/github-navigator /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

---

## 🌐 访问网站

部署完成后，访问：

```
http://你的服务器IP:5000
```

或配置 Nginx 后访问：

```
http://你的服务器IP
```

---

## 🔧 配置域名（可选）

### 1. 购买域名

在阿里云、腾讯云等平台购买域名

### 2. 配置 DNS

添加 A 记录：
- 主机记录: @
- 记录类型: A
- 记录值: 你的服务器 IP

### 3. 修改 Nginx 配置

```bash
nano /etc/nginx/sites-available/github-navigator
```

将 `server_name` 改为你的域名：
```nginx
server_name your-domain.com;
```

重启 Nginx：
```bash
systemctl restart nginx
```

### 4. 配置 SSL（免费 HTTPS）

```bash
# 安装 Certbot
apt install certbot python3-certbot-nginx -y

# 获取证书
certbot --nginx -d your-domain.com

# 自动续期
certbot renew --dry-run
```

---

## 📊 管理命令

### Docker 方式

```bash
# 查看日志
docker-compose -f /opt/github-navigator/config/docker-compose.yml logs -f

# 停止服务
docker-compose -f /opt/github-navigator/config/docker-compose.yml down

# 重启服务
docker-compose -f /opt/github-navigator/config/docker-compose.yml restart

# 更新代码
cd /opt/github-navigator
git pull
docker-compose -f config/docker-compose.yml up -d --build
```

### 传统方式

```bash
# 查看日志
journalctl -u github-navigator -f

# 停止服务
systemctl stop github-navigator

# 重启服务
systemctl restart github-navigator

# 查看状态
systemctl status github-navigator

# 更新代码
cd /var/www/github-navigator
git pull
source venv/bin/activate
pip install -r requirements.txt
systemctl restart github-navigator
```

---

## ❓ 常见问题

### Q: 端口 5000 无法访问？
A: 
1. 检查云服务器安全组是否开放 5000 端口
2. 检查防火墙：`ufw status`
3. 检查服务状态：`docker ps` 或 `systemctl status github-navigator`

### Q: 服务启动失败？
A: 
1. 查看日志找到错误原因
2. 检查端口是否被占用：`netstat -tlnp | grep 5000`
3. 检查文件权限

### Q: 如何更新数据？
A:
```bash
# Docker 方式
docker exec -it github-navigator python scripts/crawler.py
docker exec -it github-navigator python scripts/add_chinese_descriptions.py

# 传统方式
cd /var/www/github-navigator
source venv/bin/activate
python scripts/crawler.py
python scripts/add_chinese_descriptions.py
systemctl restart github-navigator
```

### Q: 内存不足？
A: 升级服务器配置到 2GB 或更高

---

## 💰 费用参考

### 阿里云 ECS
- 1核2G: ¥60-80/月
- 2核4G: ¥120-150/月

### 腾讯云 CVM
- 1核2G: ¥50-70/月
- 2核4G: ¥100-130/月

### 学生优惠
- 阿里云学生机: ¥9.5/月
- 腾讯云学生机: ¥10/月

---

## 🎯 推荐流程

1. **购买云服务器**（1核2G，Ubuntu 20.04）
2. **配置安全组**（开放 22, 80, 443, 5000 端口）
3. **运行一键部署脚本**
4. **访问网站测试**
5. **配置域名和 SSL**（可选）

---

## 📞 获取帮助

详细文档：
- `云服务器部署指南.md` - 完整部署文档
- `docs/部署指南.md` - 多种部署方式
- GitHub Issues - 提交问题

---

**现在就开始部署吧！** 🚀

只需要 5 分钟，你的网站就能上线！
