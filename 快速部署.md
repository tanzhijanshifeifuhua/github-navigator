# 🚀 快速部署指南

## 方式一：Vercel 部署（最简单）⭐

### 1. 准备工作
```bash
# 初始化 Git（如果还没有）
git init
git add .
git commit -m "Initial commit"

# 推送到 GitHub
git remote add origin https://github.com/你的用户名/你的仓库.git
git push -u origin main
```

### 2. 部署到 Vercel
1. 访问 https://vercel.com
2. 点击 "Import Project"
3. 选择你的 GitHub 仓库
4. 点击 "Deploy"
5. 完成！🎉

**访问地址**: `https://你的项目名.vercel.app`

---

## 方式二：Docker 部署（推荐本地/服务器）

### Windows
```bash
# 双击运行
deploy.bat

# 选择选项 1（Docker 部署）
```

### Linux/Mac
```bash
# 添加执行权限
chmod +x deploy.sh

# 运行部署脚本
./deploy.sh

# 选择选项 1（Docker 部署）
```

### 手动 Docker 命令
```bash
# 构建镜像
docker build -t github-navigator .

# 运行容器
docker run -d -p 5000:5000 --name github-nav github-navigator

# 访问
http://localhost:5000
```

---

## 方式三：本地开发运行

### Windows
```bash
# 安装依赖
py -m pip install -r requirements.txt

# 运行应用
py app.py
```

### Linux/Mac
```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 运行应用
python app.py
```

**访问地址**: http://localhost:5000

---

## 方式四：Railway 部署

1. 访问 https://railway.app
2. 连接 GitHub 仓库
3. 自动部署
4. 获取访问地址

---

## 常见问题

### Q: 部署后看不到数据？
A: 确保 `github_projects.json` 文件已提交到仓库

### Q: 如何更新数据？
A: 
```bash
# 本地运行爬虫
py crawler.py

# 添加中文描述
py add_chinese_descriptions.py

# 提交更新
git add github_projects.json
git commit -m "Update data"
git push
```

### Q: 如何配置自动更新？
A: 使用 GitHub Actions（已配置 `.github/workflows/update-data.yml`）

需要设置 Secret：
- `GH_TOKEN`: GitHub Personal Access Token

---

## 下一步

- [ ] 绑定自定义域名
- [ ] 配置 HTTPS
- [ ] 设置自动更新
- [ ] 添加监控

---

**需要帮助？** 查看完整的 [部署指南.md](部署指南.md)
