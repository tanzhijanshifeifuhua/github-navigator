# GitHub 项目中文分类导航 - 部署指南

## 📋 部署方式概览

本项目支持多种部署方式，根据你的需求选择：

1. **Vercel** - 最简单，免费，自动 HTTPS ⭐ 推荐
2. **Railway** - 简单，免费额度，支持数据库
3. **Render** - 免费，自动部署
4. **Docker** - 容器化部署，适合自建服务器
5. **传统服务器** - Linux VPS 部署

---

## 方案 1: Vercel 部署 ⭐ 推荐

### 优点
- 完全免费
- 自动 HTTPS
- 全球 CDN 加速
- 自动部署（Git push 即部署）
- 零配置

### 步骤

#### 1. 准备工作
确保项目已推送到 GitHub

#### 2. 安装 Vercel CLI（可选）
```bash
npm install -g vercel
```

#### 3. 在线部署（推荐）
1. 访问 https://vercel.com
2. 使用 GitHub 账号登录
3. 点击 "New Project"
4. 导入你的 GitHub 仓库
5. 配置项目：
   - Framework Preset: 选择 "Other"
   - Build Command: 留空
   - Output Directory: 留空
   - Install Command: `pip install -r requirements.txt`
6. 点击 "Deploy"

#### 4. 使用 CLI 部署
```bash
cd F:\project\github
vercel
```

按提示操作即可。

### 注意事项
- Vercel 默认使用 serverless 函数，需要调整 app.py
- 数据文件 github_projects.json 需要提交到仓库

---

## 方案 2: Railway 部署

### 优点
- 免费 $5/月额度
- 支持持久化存储
- 自动 HTTPS
- 简单易用

### 步骤

1. 访问 https://railway.app
2. 使用 GitHub 登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择你的仓库
5. Railway 会自动检测 Python 项目
6. 等待部署完成
7. 点击 "Generate Domain" 获取访问地址

### 配置文件
Railway 会自动识别，无需额外配置。

---

## 方案 3: Render 部署

### 优点
- 免费套餐
- 自动 HTTPS
- 简单配置

### 步骤

1. 访问 https://render.com
2. 注册并登录
3. 点击 "New +" → "Web Service"
4. 连接 GitHub 仓库
5. 配置：
   - Name: github-navigator
   - Environment: Python 3
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app`
6. 点击 "Create Web Service"

### 需要添加 gunicorn
在 requirements.txt 中添加：
```
gunicorn==21.2.0
```

---

## 方案 4: Docker 部署

### 优点
- 环境一致性
- 易于迁移
- 适合自建服务器

### 步骤

#### 1. 创建 Dockerfile（已为你创建）

#### 2. 构建镜像
```bash
docker build -t github-navigator .
```

#### 3. 运行容器
```bash
docker run -d -p 5000:5000 --name github-nav github-navigator
```

#### 4. 访问
打开浏览器访问 http://localhost:5000

#### 5. 使用 Docker Compose（推荐）
```bash
docker-compose up -d
```

---

## 方案 5: 传统 Linux 服务器部署

### 适用场景
- 自己的 VPS
- 云服务器（阿里云、腾讯云等）

### 步骤

#### 1. 安装依赖
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装 Python 和 pip
sudo apt install python3 python3-pip python3-venv -y

# 安装 Nginx
sudo apt install nginx -y
```

#### 2. 上传项目
```bash
# 使用 git
cd /var/www
sudo git clone https://github.com/你的用户名/你的仓库.git github-navigator
cd github-navigator

# 或使用 scp 上传
scp -r F:\project\github user@your-server:/var/www/github-navigator
```

#### 3. 创建虚拟环境
```bash
cd /var/www/github-navigator
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install gunicorn
```

#### 4. 配置 Gunicorn
创建 gunicorn 配置文件（已为你创建 gunicorn_config.py）

#### 5. 创建 Systemd 服务
创建 `/etc/systemd/system/github-navigator.service`（已为你创建模板）

```bash
sudo cp github-navigator.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl start github-navigator
sudo systemctl enable github-navigator
```

#### 6. 配置 Nginx
创建 Nginx 配置（已为你创建 nginx.conf）

```bash
sudo cp nginx.conf /etc/nginx/sites-available/github-navigator
sudo ln -s /etc/nginx/sites-available/github-navigator /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 7. 配置 SSL（可选但推荐）
```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx -y

# 获取证书
sudo certbot --nginx -d your-domain.com
```

---

## 🔧 生产环境配置

### 1. 使用生产级 WSGI 服务器

不要在生产环境使用 Flask 内置服务器，使用 Gunicorn 或 uWSGI：

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### 2. 环境变量配置

创建 `.env` 文件：
```env
FLASK_ENV=production
SECRET_KEY=your-secret-key-here
```

### 3. 日志配置

在 app.py 中添加日志：
```python
import logging
logging.basicConfig(level=logging.INFO)
```

### 4. 性能优化

- 启用 Gzip 压缩
- 使用 CDN 加速静态资源
- 添加缓存头
- 使用 Redis 缓存（可选）

---

## 📊 监控和维护

### 1. 健康检查
添加健康检查端点（已在 app.py 中添加）

### 2. 日志监控
```bash
# 查看服务日志
sudo journalctl -u github-navigator -f

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### 3. 自动更新数据
设置定时任务：
```bash
# 编辑 crontab
crontab -e

# 每天凌晨 2 点更新数据
0 2 * * * cd /var/www/github-navigator && /var/www/github-navigator/venv/bin/python crawler.py
```

---

## 🔒 安全建议

1. **使用 HTTPS** - 必须！使用 Let's Encrypt 免费证书
2. **设置防火墙** - 只开放必要端口（80, 443）
3. **定期更新** - 更新系统和依赖包
4. **限制访问** - 使用 Nginx 限流
5. **备份数据** - 定期备份 github_projects.json

---

## 🚀 快速开始（推荐流程）

### 新手推荐：Vercel
1. 将代码推送到 GitHub
2. 访问 vercel.com 导入项目
3. 一键部署，完成！

### 进阶用户：Docker
1. 安装 Docker
2. `docker-compose up -d`
3. 访问 http://localhost:5000

### 专业用户：Linux 服务器
1. 准备 VPS
2. 按照"方案 5"步骤操作
3. 配置域名和 SSL

---

## 📝 部署检查清单

- [ ] 代码已推送到 Git 仓库
- [ ] requirements.txt 包含所有依赖
- [ ] github_projects.json 已生成
- [ ] 选择部署平台
- [ ] 配置环境变量（如需要）
- [ ] 测试部署是否成功
- [ ] 配置自定义域名（可选）
- [ ] 启用 HTTPS
- [ ] 设置监控和日志
- [ ] 配置自动更新（可选）

---

## ❓ 常见问题

### Q: 部署后数据如何更新？
A: 
- 方案 1: 本地运行 crawler.py，提交更新后的 json 文件
- 方案 2: 在服务器上设置定时任务自动运行爬虫
- 方案 3: 使用 GitHub Actions 自动更新

### Q: 免费部署有限制吗？
A: 
- Vercel: 每月 100GB 带宽，足够个人使用
- Railway: $5/月免费额度
- Render: 免费套餐有休眠机制

### Q: 如何绑定自定义域名？
A: 各平台都支持自定义域名，在设置中添加域名并配置 DNS 即可

### Q: 数据更新频率建议？
A: 建议每天或每周更新一次，避免频繁请求 GitHub API

---

## 📞 获取帮助

- GitHub Issues: 在项目仓库提交问题
- 文档: 查看各平台官方文档
- 社区: Stack Overflow, Reddit

---

**祝部署顺利！** 🎉
