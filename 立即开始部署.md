# 🚀 立即开始部署到你的云服务器

## 📋 服务器信息
- **公网 IP**: 123.57.189.237
- **内网 IP**: 172.30.231.1

---

## ⚡ 最快部署方式（3分钟）

### 步骤 1：连接服务器

在你的电脑上打开终端（PowerShell/CMD/Terminal），输入：

```bash
ssh root@123.57.189.237
```

输入密码后回车。

---

### 步骤 2：运行一键部署脚本

连接成功后，复制并运行以下命令：

```bash
curl -fsSL https://raw.githubusercontent.com/tanzhijanshifeifuhua/github-navigator/main/scripts/auto_deploy.sh -o deploy.sh && chmod +x deploy.sh && sudo bash deploy.sh
```

**如果 curl 不可用，使用 wget：**
```bash
wget https://raw.githubusercontent.com/tanzhijanshifeifuhua/github-navigator/main/scripts/auto_deploy.sh -O deploy.sh && chmod +x deploy.sh && sudo bash deploy.sh
```

---

### 步骤 3：选择部署方式

脚本会提示你选择：

```
请选择部署方式:
1. Docker 部署（推荐）
2. 传统部署
请输入选项 (1-2):
```

**输入 `1` 然后回车**（推荐 Docker 部署）

---

### 步骤 4：等待完成

脚本会自动：
- ✅ 安装 Docker
- ✅ 克隆项目
- ✅ 启动服务
- ✅ 配置防火墙

大约 3-5 分钟完成。

---

### 步骤 5：访问网站

部署完成后，在浏览器访问：

```
http://123.57.189.237:5000
```

🎉 **完成！你的网站已经上线了！**

---

## 🔧 重要：开放端口

如果无法访问，需要在云服务器控制台开放端口：

### 阿里云 ECS
1. 登录阿里云控制台
2. 进入 ECS 实例
3. 点击"安全组"
4. 添加安全组规则：
   - 端口范围：5000/5000
   - 授权对象：0.0.0.0/0
   - 协议：TCP

### 腾讯云 CVM
1. 登录腾讯云控制台
2. 进入云服务器
3. 点击"安全组"
4. 添加入站规则：
   - 端口：5000
   - 来源：0.0.0.0/0
   - 协议：TCP

### 华为云 ECS
1. 登录华为云控制台
2. 进入弹性云服务器
3. 点击"安全组"
4. 添加入方向规则：
   - 端口：5000
   - 源地址：0.0.0.0/0
   - 协议：TCP

---

## 📊 验证部署

### 在服务器上测试

```bash
# 测试健康检查
curl http://localhost:5000/health

# 应该返回类似：
# {"status":"healthy","total_projects":1158,"updated_at":"..."}
```

### 在本地测试

在你的电脑浏览器访问：
```
http://123.57.189.237:5000
```

或使用 curl：
```bash
curl http://123.57.189.237:5000/health
```

---

## 🛠️ 常用管理命令

连接到服务器后，可以使用这些命令：

### 查看服务状态
```bash
docker ps
```

### 查看日志
```bash
docker-compose -f /opt/github-navigator/config/docker-compose.yml logs -f
```

### 重启服务
```bash
docker-compose -f /opt/github-navigator/config/docker-compose.yml restart
```

### 停止服务
```bash
docker-compose -f /opt/github-navigator/config/docker-compose.yml down
```

### 更新代码
```bash
cd /opt/github-navigator
git pull
docker-compose -f config/docker-compose.yml up -d --build
```

---

## 🌐 配置域名（可选）

如果你有域名，可以绑定到这个服务器：

### 1. 配置 DNS
在域名管理后台添加 A 记录：
- 主机记录：@ 或 www
- 记录类型：A
- 记录值：123.57.189.237
- TTL：600

### 2. 配置 Nginx
```bash
# 安装 Nginx
apt install nginx -y

# 修改配置
nano /etc/nginx/sites-available/github-navigator

# 将 server_name 改为你的域名
# server_name your-domain.com;

# 启用配置
ln -s /etc/nginx/sites-available/github-navigator /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
```

### 3. 配置 SSL（免费 HTTPS）
```bash
apt install certbot python3-certbot-nginx -y
certbot --nginx -d your-domain.com
```

---

## ❓ 故障排除

### 问题 1：无法访问 http://123.57.189.237:5000

**解决方案：**
1. 检查服务是否运行：`docker ps`
2. 检查端口是否监听：`netstat -tlnp | grep 5000`
3. 检查云服务器安全组是否开放 5000 端口
4. 检查服务器防火墙：`ufw status`

### 问题 2：服务启动失败

**解决方案：**
```bash
# 查看详细日志
docker-compose -f /opt/github-navigator/config/docker-compose.yml logs

# 检查端口占用
netstat -tlnp | grep 5000

# 如果端口被占用，停止占用进程
kill -9 <PID>
```

### 问题 3：数据显示为空

**解决方案：**
```bash
# 检查数据文件
docker exec github-navigator ls -lh /app/github_projects.json

# 如果文件不存在，重新生成
docker exec -it github-navigator python /app/scripts/crawler.py
docker exec -it github-navigator python /app/scripts/add_chinese_descriptions.py
docker-compose -f /opt/github-navigator/config/docker-compose.yml restart
```

---

## 📞 需要帮助？

查看详细文档：
- `部署命令.txt` - 所有命令汇总
- `云服务器部署指南.md` - 完整部署教程
- `部署到172.30.231.1.md` - 详细步骤说明

---

## 🎯 下一步

部署成功后，你可以：

1. ✅ 分享网站给朋友：http://123.57.189.237:5000
2. ✅ 配置域名和 HTTPS
3. ✅ 设置自动更新数据
4. ✅ 优化性能和安全

---

**现在就开始部署吧！** 🚀

只需要 3 分钟，你的网站就能上线！
